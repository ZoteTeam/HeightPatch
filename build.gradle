plugins {
    id 'java'
}

group = 'org.zoteteam.height_patch'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()

    maven {
        name "innercoreReleases"
        url "https://maven.inner-core.org/releases"
    }
}

dependencies {
    compileOnly "org.innercore.zotecoreloader:ZoteCoreLoader:beta-1.1.1"
}